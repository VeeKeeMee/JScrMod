document.querySelector('button').addEventListener('click', Submit);

function Submit() {
    // value - get the text from input
    // innerHTML - get the text on p, body
    let getInput = document.querySelector('.i-1').value;
    console.log(getInput);

}